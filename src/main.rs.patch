--- a/src/main.rs
+++ b/src/main.rs
@@ -1,3 +1,6 @@
+// 增加栈大小以支持更深的递归（临时方案）
+#[global_allocator]
+static GLOBAL: std::alloc::System = std::alloc::System;
+
 use std::io::{self, Write, BufRead, BufReader};
 use std::fs::File;
 use std::env;
